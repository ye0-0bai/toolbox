[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "toolbox"
version = "0.1.0"
description = ""
readme = "README.md"
requires-python = ">=3.8"
authors = [
    { name = "yebai", email = "704535747@qq.com" }
]

dependencies = [
    "tqdm",
    "h5py",
    "numpy<2",
    "imageio[ffmpeg]",
    "opencv-python",
    "trimesh",
    "pyrender",
]

[tool.setuptools]
packages = ["toolbox"]